This directory is used to host HL2SDKs.

If you are on Windows, use the src/Build.bat file to retrieve the
    proper hl2sdk branch and create the build files.

If you are on Linux, use the src/Build.sh file to retrieve the
    proper hl2sdk branch and automatically build the binaries.
